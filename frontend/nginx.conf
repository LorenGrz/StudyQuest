server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: any unknown path is served by index.html so the
    # client-side router (React Router) can resolve it.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
